import styles from "./recom.module.scss";

const Recommendation =()=>{
    return(
        <div className={styles.root}>
            <h1 className={styles.title}>Аналогичные товары</h1>
        </div>
    )
};

export default Recommendation;